const sr = ScrollReveal({
    origin: 'top',
    distance: '30px',
    duration: 2000,
    reset: true,
})

sr.reveal(`#hero, #banner, #features, #caracteristicas, #cartas, #equipos, #moviles, #precios`, {
    interval: 200,
})